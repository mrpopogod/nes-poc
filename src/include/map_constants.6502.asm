MAP_HEADERS = $8000             ; Starting address of the map header table in all map banks

; Defs of where maps are so they can be referenced by other maps
TEST_MAP_BANK = $00             ; Defined in bank 0
TEST_MAP_OFFSET = $00           ; First map in the bank